diff --git a/src-tauri/src/browser_runner.rs b/src-tauri/src/browser_runner.rs
index d7b9855..a3aef1a 100644
--- a/src-tauri/src/browser_runner.rs
+++ b/src-tauri/src/browser_runner.rs
@@ -690,6 +690,21 @@ impl BrowserRunner {

           // Launch Camoufox with -profile and -new-tab to open URL in existing instance
           // This works because we no longer use -no-remote flag
+          #[cfg(target_os = "linux")]
+          let output = {
+            let args = vec![
+              "-profile".to_string(),
+              profile_path_str.to_string(),
+              "-new-tab".to_string(),
+              url.to_string(),
+            ];
+            crate::platform_browser::linux::create_linux_command(&executable_path, &args)
+              .map_err(|e| format!("Failed to prepare Camoufox runtime: {e}"))?
+              .output()
+              .map_err(|e| format!("Failed to execute Camoufox: {e}"))?
+          };
+
+          #[cfg(not(target_os = "linux"))]
           let output = std::process::Command::new(&executable_path)
             .arg("-profile")
             .arg(&*profile_path_str)
diff --git a/src-tauri/src/camoufox_manager.rs b/src-tauri/src/camoufox_manager.rs
index 2d2229f..ab3786f 100644
--- a/src-tauri/src/camoufox_manager.rs
+++ b/src-tauri/src/camoufox_manager.rs
@@ -1,5 +1,7 @@
 use crate::browser_runner::BrowserRunner;
 use crate::camoufox::{CamoufoxConfigBuilder, GeoIPOption, ScreenConstraints};
+#[cfg(target_os = "linux")]
+use crate::platform_browser::linux::create_linux_tokio_command;
 use crate::profile::BrowserProfile;
 use serde::{Deserialize, Serialize};
 use std::collections::HashMap;
@@ -7,7 +9,6 @@ use std::path::PathBuf;
 use std::process::Stdio;
 use std::sync::Arc;
 use tauri::AppHandle;
-use tokio::process::Command as TokioCommand;
 use tokio::sync::Mutex as AsyncMutex;

 #[derive(Debug, Clone, Serialize, Deserialize)]
@@ -263,9 +264,16 @@ impl CamoufoxManager {
     );

     // Spawn the browser process
-    let mut command = TokioCommand::new(&executable_path);
+    #[cfg(target_os = "linux")]
+    let mut command = create_linux_tokio_command(&executable_path, &args)?;
+    #[cfg(not(target_os = "linux"))]
+    let mut command = {
+      let mut command = tokio::process::Command::new(&executable_path);
+      command.args(&args);
+      command
+    };
+
     command
-      .args(&args)
       .stdin(Stdio::null())
       .stdout(Stdio::null())
       .stderr(Stdio::null());
diff --git a/src-tauri/src/extraction.rs b/src-tauri/src/extraction.rs
index 349b60d..d386369 100644
--- a/src-tauri/src/extraction.rs
+++ b/src-tauri/src/extraction.rs
@@ -221,6 +221,11 @@ impl Extractor {
             .await;
         }

+        #[cfg(target_os = "linux")]
+        {
+          crate::platform_browser::linux::prepare_linux_browser_runtime(&path)?;
+        }
+
         log::info!(
           "Successfully extracted {} {} to: {}",
           browser_type.as_str(),
diff --git a/src-tauri/src/platform_browser.rs b/src-tauri/src/platform_browser.rs
index 9814225..6d65bd6 100644
--- a/src-tauri/src/platform_browser.rs
+++ b/src-tauri/src/platform_browser.rs
@@ -685,93 +685,367 @@ pub mod windows {
 #[allow(dead_code)]
 pub mod linux {
   use super::*;
+  use std::collections::HashSet;
+  use std::fs::File;
+  use std::io::Read;
+  use std::path::PathBuf;
+  use tokio::process::Command as TokioCommand;
+
+  enum LinuxEnvAction {
+    Set(&'static str, String),
+    Remove(&'static str),
+  }
+
+  #[cfg(target_family = "unix")]
+  fn is_elf_executable(path: &Path) -> Result<bool, Box<dyn std::error::Error + Send + Sync>> {
+    use std::os::unix::fs::PermissionsExt;
+
+    let metadata = std::fs::metadata(path)?;
+    if !metadata.is_file() || metadata.permissions().mode() & 0o111 == 0 {
+      return Ok(false);
+    }
+
+    let mut header = [0u8; 4];
+    let mut file = File::open(path)?;
+    if file.read_exact(&mut header).is_err() {
+      return Ok(false);
+    }
+
+    Ok(header == [0x7f, b'E', b'L', b'F'])
+  }
+
+  fn resolve_patchelf_bin() -> Option<PathBuf> {
+    if let Ok(path) = std::env::var("DONUT_PATCHELF_BIN") {
+      let path = PathBuf::from(path);
+      if path.exists() {
+        return Some(path);
+      }
+    }
+
+    let Ok(path_var) = std::env::var("PATH") else {
+      return None;
+    };
+
+    std::env::split_paths(&path_var)
+      .map(|dir| dir.join("patchelf"))
+      .find(|path| path.exists())
+  }
+
+  fn read_elf_interpreter(
+    patchelf_bin: &Path,
+    path: &Path,
+  ) -> Result<Option<String>, Box<dyn std::error::Error + Send + Sync>> {
+    let output = Command::new(patchelf_bin)
+      .arg("--print-interpreter")
+      .arg(path)
+      .output()?;
+
+    if !output.status.success() {
+      return Ok(None);
+    }
+
+    let interpreter = String::from_utf8_lossy(&output.stdout).trim().to_string();
+    if interpreter.is_empty() {
+      Ok(None)
+    } else {
+      Ok(Some(interpreter))
+    }
+  }
+
+  fn patch_elf_interpreter(
+    patchelf_bin: &Path,
+    path: &Path,
+    target_interpreter: &str,
+  ) -> Result<bool, Box<dyn std::error::Error + Send + Sync>> {
+    let Some(current_interpreter) = read_elf_interpreter(patchelf_bin, path)? else {
+      return Ok(false);
+    };
+
+    if current_interpreter == target_interpreter {
+      return Ok(false);
+    }
+
+    let output = Command::new(patchelf_bin)
+      .arg("--set-interpreter")
+      .arg(target_interpreter)
+      .arg(path)
+      .output()?;
+
+    if !output.status.success() {
+      return Err(
+        format!(
+          "Failed to patch ELF interpreter for {}: {}",
+          path.display(),
+          String::from_utf8_lossy(&output.stderr)
+        )
+        .into(),
+      );
+    }

-  pub async fn launch_browser_process(
-    executable_path: &std::path::Path,
-    args: &[String],
-  ) -> Result<std::process::Child, Box<dyn std::error::Error + Send + Sync>> {
     log::info!(
-      "Launching browser on Linux: {:?} with args: {:?}",
-      executable_path,
-      args
+      "Patched ELF interpreter for {}: {} -> {}",
+      path.display(),
+      current_interpreter,
+      target_interpreter
     );
+    Ok(true)
+  }

-    // Check if the executable exists and is executable
-    if !executable_path.exists() {
-      return Err(format!("Browser executable not found: {:?}", executable_path).into());
+  fn collect_elf_candidates(
+    dir: &Path,
+    candidates: &mut Vec<PathBuf>,
+  ) -> Result<(), Box<dyn std::error::Error + Send + Sync>> {
+    for entry in std::fs::read_dir(dir)? {
+      let entry = entry?;
+      let path = entry.path();
+
+      if path.is_dir() {
+        collect_elf_candidates(&path, candidates)?;
+        continue;
+      }
+
+      if is_elf_executable(&path)? {
+        candidates.push(path);
+      }
     }

-    // Check if we can read the executable to detect architecture issues early
-    if let Err(e) = std::fs::File::open(executable_path) {
-      return Err(format!("Cannot access browser executable: {}", e).into());
+    Ok(())
+  }
+
+  pub(crate) fn prepare_linux_browser_runtime(
+    executable_path: &Path,
+  ) -> Result<(), Box<dyn std::error::Error + Send + Sync>> {
+    let Some(target_interpreter) = std::env::var("NIX_LD")
+      .ok()
+      .filter(|value| !value.is_empty())
+    else {
+      return Ok(());
+    };
+
+    let Some(patchelf_bin) = resolve_patchelf_bin() else {
+      return Err("DONUT_PATCHELF_BIN is not set and patchelf was not found in PATH".into());
+    };
+
+    let bundle_dir = executable_path.parent().ok_or_else(|| {
+      format!(
+        "Cannot determine browser bundle directory for {}",
+        executable_path.display()
+      )
+    })?;
+
+    let mut candidates = Vec::new();
+    collect_elf_candidates(bundle_dir, &mut candidates)?;
+
+    let mut unique_paths = Vec::new();
+    let mut seen_paths = HashSet::new();
+    for path in candidates {
+      if seen_paths.insert(path.clone()) {
+        unique_paths.push(path);
+      }
     }

-    // Ensure the executable has proper permissions
-    if let Err(e) = std::fs::metadata(executable_path) {
-      return Err(format!("Cannot get executable metadata: {}", e).into());
+    let mut patched_files = 0usize;
+    let mut inspected_files = 0usize;
+    for path in unique_paths {
+      if read_elf_interpreter(&patchelf_bin, &path)?.is_some() {
+        inspected_files += 1;
+        patched_files += usize::from(patch_elf_interpreter(
+          &patchelf_bin,
+          &path,
+          &target_interpreter,
+        )?);
+      }
     }

-    // On Linux, we might need to set LD_LIBRARY_PATH for some browsers
-    let mut cmd = Command::new(executable_path);
-    cmd.args(args);
+    log::info!(
+      "Linux browser runtime prepared for {} ({} ELF executables inspected, {} patched)",
+      executable_path.display(),
+      inspected_files,
+      patched_files
+    );

-    // For Firefox-based browsers, ensure library path includes the installation directory
-    if let Some(install_dir) = executable_path.parent() {
-      let mut ld_library_path = Vec::new();
+    Ok(())
+  }

-      // Add multiple potential library directories
-      let lib_dirs = [
-        install_dir.join("lib"),
-        install_dir.join("../lib"),    // Parent directory lib
-        install_dir.join("../../lib"), // Grandparent directory lib
-        install_dir.to_path_buf(),     // Installation directory itself
-      ];
+  fn collect_linux_library_path(executable_path: &Path) -> Option<String> {
+    let Some(install_dir) = executable_path.parent() else {
+      return None;
+    };

-      for lib_dir in &lib_dirs {
-        if lib_dir.exists() {
-          ld_library_path.push(lib_dir.to_string_lossy().to_string());
-        }
+    let mut library_path = Vec::new();
+    let mut seen_paths = HashSet::new();
+    let mut push_path = |path: String| {
+      if !path.is_empty() && seen_paths.insert(path.clone()) {
+        library_path.push(path);
+      }
+    };
+
+    let lib_dirs = [
+      install_dir.join("lib"),
+      install_dir.join("../lib"),
+      install_dir.join("../../lib"),
+      install_dir.to_path_buf(),
+    ];
+
+    for lib_dir in &lib_dirs {
+      if lib_dir.exists() {
+        push_path(lib_dir.to_string_lossy().to_string());
       }
+    }

-      // For Firefox specifically, add common system library paths that might be needed
-      let firefox_lib_paths = [
-        "/usr/lib/firefox",
-        "/usr/lib/x86_64-linux-gnu",
-        "/usr/lib/aarch64-linux-gnu",
-        "/lib/x86_64-linux-gnu",
-        "/lib/aarch64-linux-gnu",
-      ];
+    let firefox_lib_paths = [
+      "/usr/lib/firefox",
+      "/usr/lib/x86_64-linux-gnu",
+      "/usr/lib/aarch64-linux-gnu",
+      "/lib/x86_64-linux-gnu",
+      "/lib/aarch64-linux-gnu",
+    ];
+
+    for lib_path in &firefox_lib_paths {
+      if Path::new(lib_path).exists() {
+        push_path((*lib_path).to_string());
+      }
+    }

-      for lib_path in &firefox_lib_paths {
-        let path = std::path::Path::new(lib_path);
-        if path.exists() {
-          ld_library_path.push(lib_path.to_string());
+    for env_var in ["NIX_LD_LIBRARY_PATH", "LD_LIBRARY_PATH"] {
+      if let Ok(existing_path) = std::env::var(env_var) {
+        if !existing_path.is_empty() {
+          for path in existing_path.split(':') {
+            push_path(path.to_string());
+          }
         }
       }
+    }
+
+    (!library_path.is_empty()).then(|| library_path.join(":"))
+  }
+
+  fn collect_common_linux_env(library_path: Option<&str>) -> Vec<LinuxEnvAction> {
+    let mut actions = Vec::new();

-      // Preserve existing LD_LIBRARY_PATH
-      if let Ok(existing_path) = std::env::var("LD_LIBRARY_PATH") {
-        ld_library_path.push(existing_path);
+    if let Some(library_path) = library_path {
+      actions.push(LinuxEnvAction::Set(
+        "LD_LIBRARY_PATH",
+        library_path.to_string(),
+      ));
+      log::info!("Set LD_LIBRARY_PATH to: {library_path}");
+    }
+
+    for var in [
+      "NIX_LD",
+      "NIX_LD_LIBRARY_PATH",
+      "WAYLAND_DISPLAY",
+      "XDG_RUNTIME_DIR",
+      "DBUS_SESSION_BUS_ADDRESS",
+      "XDG_SESSION_TYPE",
+      "XDG_CURRENT_DESKTOP",
+      "PLAYWRIGHT_NODEJS_PATH",
+      "GDK_BACKEND",
+      "MOZ_ENABLE_WAYLAND",
+    ] {
+      if let Ok(value) = std::env::var(var) {
+        actions.push(LinuxEnvAction::Set(var, value));
       }
+    }
+
+    if let Ok(display) = std::env::var("DISPLAY") {
+      actions.push(LinuxEnvAction::Set("DISPLAY", display));
+    } else {
+      actions.push(LinuxEnvAction::Remove("DISPLAY"));
+    }
+
+    if std::env::var("WAYLAND_DISPLAY").is_ok() {
+      actions.push(LinuxEnvAction::Set("MOZ_ENABLE_WAYLAND", "1".to_string()));
+    }
+
+    actions
+  }
+
+  pub(crate) fn create_linux_command(
+    executable_path: &Path,
+    args: &[String],
+  ) -> Result<Command, Box<dyn std::error::Error + Send + Sync>> {
+    prepare_linux_browser_runtime(executable_path)?;
+
+    log::info!(
+      "Launching {} directly after Linux runtime preparation",
+      executable_path.display()
+    );
+
+    let mut cmd = Command::new(executable_path);
+    cmd.args(args);

-      // Set the combined LD_LIBRARY_PATH
-      if !ld_library_path.is_empty() {
-        cmd.env("LD_LIBRARY_PATH", ld_library_path.join(":"));
-        log::info!("Set LD_LIBRARY_PATH to: {}", ld_library_path.join(":"));
+    let library_path = collect_linux_library_path(executable_path);
+    for action in collect_common_linux_env(library_path.as_deref()) {
+      match action {
+        LinuxEnvAction::Set(key, value) => {
+          cmd.env(key, value);
+        }
+        LinuxEnvAction::Remove(key) => {
+          cmd.env_remove(key);
+        }
       }
     }

-    // Additional Linux-specific environment variables for better compatibility
-    cmd.env(
-      "DISPLAY",
-      std::env::var("DISPLAY").unwrap_or(":0".to_string()),
+    Ok(cmd)
+  }
+
+  pub(crate) fn create_linux_tokio_command(
+    executable_path: &Path,
+    args: &[String],
+  ) -> Result<TokioCommand, Box<dyn std::error::Error + Send + Sync>> {
+    prepare_linux_browser_runtime(executable_path)?;
+
+    log::info!(
+      "Launching {} directly after Linux runtime preparation",
+      executable_path.display()
     );

-    // Set MOZ_ENABLE_WAYLAND for better Wayland support
-    if std::env::var("WAYLAND_DISPLAY").is_ok() {
-      cmd.env("MOZ_ENABLE_WAYLAND", "1");
+    let mut cmd = TokioCommand::new(executable_path);
+    cmd.args(args);
+
+    let library_path = collect_linux_library_path(executable_path);
+    for action in collect_common_linux_env(library_path.as_deref()) {
+      match action {
+        LinuxEnvAction::Set(key, value) => {
+          cmd.env(key, value);
+        }
+        LinuxEnvAction::Remove(key) => {
+          cmd.env_remove(key);
+        }
+      }
+    }
+
+    Ok(cmd)
+  }
+
+  pub async fn launch_browser_process(
+    executable_path: &std::path::Path,
+    args: &[String],
+  ) -> Result<std::process::Child, Box<dyn std::error::Error + Send + Sync>> {
+    log::info!(
+      "Launching browser on Linux: {:?} with args: {:?}",
+      executable_path,
+      args
+    );
+
+    // Check if the executable exists and is executable
+    if !executable_path.exists() {
+      return Err(format!("Browser executable not found: {:?}", executable_path).into());
     }

+    // Check if we can read the executable to detect architecture issues early
+    if let Err(e) = std::fs::File::open(executable_path) {
+      return Err(format!("Cannot access browser executable: {}", e).into());
+    }
+
+    // Ensure the executable has proper permissions
+    if let Err(e) = std::fs::metadata(executable_path) {
+      return Err(format!("Cannot get executable metadata: {}", e).into());
+    }
+
+    let mut cmd = create_linux_command(executable_path, args)?;
+
     // Disable GPU acceleration if running in headless environments
     if std::env::var("DISPLAY").is_err() || std::env::var("WAYLAND_DISPLAY").is_err() {
       log::info!("No display detected, browser may fail to start");
@@ -820,14 +1094,13 @@ pub mod linux {
       .map_err(|e| format!("Failed to get executable path: {}", e))?;

     let profile_data_path = profile.get_profile_data_path(profiles_dir);
-    let output = Command::new(executable_path)
-      .args([
-        "-profile",
-        &profile_data_path.to_string_lossy(),
-        "-new-tab",
-        url,
-      ])
-      .output()?;
+    let args = vec![
+      "-profile".to_string(),
+      profile_data_path.to_string_lossy().to_string(),
+      "-new-tab".to_string(),
+      url.to_string(),
+    ];
+    let output = create_linux_command(&executable_path, &args)?.output()?;

     if !output.status.success() {
       return Err(
@@ -855,12 +1128,11 @@ pub mod linux {
       .map_err(|e| format!("Failed to get executable path: {}", e))?;

     let profile_data_path = profile.get_profile_data_path(profiles_dir);
-    let output = Command::new(executable_path)
-      .args([
-        &format!("--user-data-dir={}", profile_data_path.to_string_lossy()),
-        url,
-      ])
-      .output()?;
+    let args = vec![
+      format!("--user-data-dir={}", profile_data_path.to_string_lossy()),
+      url.to_string(),
+    ];
+    let output = create_linux_command(&executable_path, &args)?.output()?;

     if !output.status.success() {
       return Err(
diff --git a/src-tauri/src/wayfern_manager.rs b/src-tauri/src/wayfern_manager.rs
index 01b59a3..21a035c 100644
--- a/src-tauri/src/wayfern_manager.rs
+++ b/src-tauri/src/wayfern_manager.rs
@@ -1,14 +1,18 @@
 use crate::browser_runner::BrowserRunner;
+#[cfg(target_os = "linux")]
+use crate::platform_browser::linux::create_linux_tokio_command;
 use crate::profile::BrowserProfile;
 use reqwest::Client;
 use serde::{Deserialize, Serialize};
 use serde_json::json;
 use std::collections::HashMap;
+use std::path::Path;
 use std::path::PathBuf;
 use std::process::Stdio;
 use std::sync::Arc;
 use std::time::Duration;
 use tauri::AppHandle;
+use tokio::io::AsyncReadExt;
 use tokio::process::Command as TokioCommand;
 use tokio::sync::Mutex as AsyncMutex;
 use tokio_tungstenite::{connect_async, tungstenite::Message};
@@ -131,13 +135,44 @@ impl WayfernManager {
     fingerprint
   }

-  async fn wait_for_cdp_ready(
+  #[cfg(target_os = "linux")]
+  fn create_browser_command(
+    executable_path: &Path,
+    args: &[String],
+  ) -> Result<TokioCommand, Box<dyn std::error::Error + Send + Sync>> {
+    create_linux_tokio_command(executable_path, args)
+  }
+
+  #[cfg(not(target_os = "linux"))]
+  fn create_browser_command(
+    executable_path: &Path,
+    args: &[String],
+  ) -> Result<TokioCommand, Box<dyn std::error::Error + Send + Sync>> {
+    let mut cmd = TokioCommand::new(executable_path);
+    cmd.args(args);
+    Ok(cmd)
+  }
+
+  async fn read_child_stream(stream: &mut Option<tokio::process::ChildStderr>) -> String {
+    let Some(stderr) = stream.as_mut() else {
+      return String::new();
+    };
+
+    let mut bytes = Vec::new();
+    if stderr.read_to_end(&mut bytes).await.is_ok() {
+      String::from_utf8_lossy(&bytes).trim().to_string()
+    } else {
+      String::new()
+    }
+  }
+
+  async fn wait_for_cdp_ready_with_child(
     &self,
     port: u16,
+    child: &mut tokio::process::Child,
+    executable_path: &Path,
   ) -> Result<(), Box<dyn std::error::Error + Send + Sync>> {
     let url = format!("http://127.0.0.1:{port}/json/version");
-    // On first launch, macOS Gatekeeper verifies the binary which can take 30+ seconds.
-    // Use a generous timeout (60s) to handle this.
     let max_attempts = 120;
     let delay = Duration::from_millis(500);

@@ -148,6 +183,23 @@ impl WayfernManager {
           return Ok(());
         }
         _ => {
+          if let Some(status) = child.try_wait()? {
+            let stderr = Self::read_child_stream(&mut child.stderr).await;
+            let stderr = if stderr.is_empty() {
+              "No stderr output captured".to_string()
+            } else {
+              stderr
+            };
+            return Err(
+              format!(
+                "Wayfern exited before CDP became ready (status: {}). Executable: {}. Stderr: {}",
+                status,
+                executable_path.display(),
+                stderr
+              )
+              .into(),
+            );
+          }
           tokio::time::sleep(delay).await;
         }
       }
@@ -223,29 +275,32 @@ impl WayfernManager {
       std::env::temp_dir().join(format!("wayfern_fingerprint_{}", uuid::Uuid::new_v4()));
     std::fs::create_dir_all(&temp_profile_dir)?;

-    let mut cmd = TokioCommand::new(&executable_path);
-    cmd
-      .arg("--headless=new")
-      .arg(format!("--remote-debugging-port={port}"))
-      .arg("--remote-debugging-address=127.0.0.1")
-      .arg(format!("--user-data-dir={}", temp_profile_dir.display()))
-      .arg("--disable-gpu")
-      .arg("--no-first-run")
-      .arg("--no-default-browser-check")
-      .arg("--disable-background-mode")
-      .arg("--use-mock-keychain")
-      .arg("--password-store=basic")
-      .arg("--disable-features=DialMediaRouteProvider");
+    let mut args = vec![
+      "--headless=new".to_string(),
+      format!("--remote-debugging-port={port}"),
+      "--remote-debugging-address=127.0.0.1".to_string(),
+      format!("--user-data-dir={}", temp_profile_dir.display()),
+      "--disable-gpu".to_string(),
+      "--no-first-run".to_string(),
+      "--no-default-browser-check".to_string(),
+      "--disable-background-mode".to_string(),
+      "--use-mock-keychain".to_string(),
+      "--password-store=basic".to_string(),
+      "--disable-features=DialMediaRouteProvider".to_string(),
+    ];

     #[cfg(target_os = "linux")]
-    cmd
-      .arg("--no-sandbox")
-      .arg("--disable-setuid-sandbox")
-      .arg("--disable-dev-shm-usage");
+    {
+      args.push("--no-sandbox".to_string());
+      args.push("--disable-setuid-sandbox".to_string());
+      args.push("--disable-dev-shm-usage".to_string());
+    }
+
+    let mut cmd = Self::create_browser_command(&executable_path, &args)?;

-    cmd.stdout(Stdio::null()).stderr(Stdio::null());
+    cmd.stdout(Stdio::null()).stderr(Stdio::piped());

-    let child = cmd.spawn()?;
+    let mut child = cmd.spawn()?;
     let child_id = child.id();

     let cleanup = || async {
@@ -269,7 +324,10 @@ impl WayfernManager {
       let _ = std::fs::remove_dir_all(&temp_profile_dir);
     };

-    if let Err(e) = self.wait_for_cdp_ready(port).await {
+    if let Err(e) = self
+      .wait_for_cdp_ready_with_child(port, &mut child, &executable_path)
+      .await
+    {
       cleanup().await;
       return Err(e);
     }
@@ -592,15 +650,16 @@ impl WayfernManager {
     // Don't add URL to args - we'll navigate via CDP after setting fingerprint
     // This ensures fingerprint is applied at navigation commit time

-    let mut cmd = TokioCommand::new(&executable_path);
-    cmd.args(&args);
+    let mut cmd = Self::create_browser_command(&executable_path, &args)?;
     cmd.stdout(Stdio::piped());
     cmd.stderr(Stdio::piped());

-    let child = cmd.spawn()?;
+    let mut child = cmd.spawn()?;
     let process_id = child.id();

-    self.wait_for_cdp_ready(port).await?;
+    self
+      .wait_for_cdp_ready_with_child(port, &mut child, &executable_path)
+      .await?;

     // Get CDP targets first - needed for both fingerprint and navigation
     let targets = self.get_cdp_targets(port).await?;
